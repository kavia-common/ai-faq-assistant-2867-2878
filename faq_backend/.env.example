# Environment variables for AI FAQ Backend
# IMPORTANT: Do not commit real secrets. The orchestrator will set these in .env.

# Django
DJANGO_SECRET_KEY=replace_me

# RAG/vector/generation config (placeholders for future managed services)
VECTOR_BACKEND=tfidf
GENERATION_MODEL=local

# Database (future external DB component support)
# Example if using Postgres via separate DB container:
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME
